coverage:
  status:
    project:
      default:
        target: 85%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
    patch:
      default:
        target: 85%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
